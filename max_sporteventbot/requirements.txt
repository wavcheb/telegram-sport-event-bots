# Core dependencies
python-dotenv>=1.0.0
loguru>=0.7.0
parsedatetime>=2.6
recurrent>=0.4.1

# MAX Bot API - official library
# 1.2.2 has set_commands(); older versions only have the deprecated
# change_info()/set_my_commands() path, which MAX is retiring.
maxapi>=1.2.2

# Database
mysql-connector-python>=8.0.33

# HTTP client for cross-platform sync (SOCKS proxy support via PySocks)
requests[socks]>=2.28.0
